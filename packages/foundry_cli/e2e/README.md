# foundry_cli_e2e

End-to-end tests for the `foundry_cli` package. Tests spawn the CLI and assert
on generated artifacts.

Run from the repository root once Melos e2e scripts are configured:

```bash
melos run test.e2e
```
